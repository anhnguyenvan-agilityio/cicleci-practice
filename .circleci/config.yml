version: 2
jobs:
  build:
    docker:
      - image: node:11.10.1
    working_directory: ~/my-repos
    branches:
      only:
        - master
    steps:
      - checkout
      - run:
          command: cd my-app && npm install && npm run test && npm run build && npm run netlify:deploy
